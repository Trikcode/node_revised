const CustomAPIError = require('./custom-error')
const BadRequestError = require('./badrequest')
const UnauthenticatedError = require('./unauthentucated')

module.exports = {
  CustomAPIError,
  BadRequestError,
  UnauthenticatedError,
}
